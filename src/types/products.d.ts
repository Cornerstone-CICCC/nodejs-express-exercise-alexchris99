// create an interface with the structure we want to follow in the array of products and export it
export interface Product{
    id: string,
    productName: string,
    productDescription: string,
    productPrice: number
}